import 'package:flutter/material.dart';

abstract class AppUIConstants {
  static const borderCircular6 = BorderRadius.all(Radius.circular(6));
  static const borderCircular30 = BorderRadius.all(Radius.circular(30));
}
